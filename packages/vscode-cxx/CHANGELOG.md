# Changelog

## 0.1.0

- Initial release.
- Injection grammar that highlights CSS inside `cxx\`...\`` and `cxx.tag<T>('name')\`...\`` tagged template literals across TypeScript, TSX, JavaScript, JSX and the `.mts` / `.mjs` / `.cjs` variants.